/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.coinstats.app",
      "i.pinimg.com",
      "image.tmdb.org",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
